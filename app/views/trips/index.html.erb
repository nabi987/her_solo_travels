<h1>View all trips</h1>
<ul>
  <% @trips.each do |trip| %>
    <li><%= link_to trip.name, trip_path(trip) %></li>
  <% end %>
</ul>

<%= link_to "Add a trip", new_trip_path, class: "btn btn-outline-primary" %>
